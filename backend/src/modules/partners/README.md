# Módulo: partners

CRUD de parceiros (apenas admin).

Endpoints típicos:

- `GET /admin/partners`
- `POST /admin/partners`
- `PATCH /admin/partners/:id`
- `DELETE /admin/partners/:id` → soft delete (RN003)
